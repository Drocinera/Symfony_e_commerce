{% extends 'base.html.twig' %}

{% block body %}
    <h1>Modifier le Produit : {{ sweatshirt.name }}</h1>

    {{ form_start(editForm) }}
        {{ form_widget(editForm) }}
        <button type="submit" class="btn">Modifier</button>
    {{ form_end(editForm) }}

    <a href="{{ path('admin_index') }}" class="btn">Retour à la liste</a>
{% endblock %}
