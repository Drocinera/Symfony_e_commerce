{% extends 'base.html.twig' %}

{% block body %}
    <h1>Bienvenue sur notre boutique de sweat-shirts</h1>

    <section class="highlighted-products">
        <h2>Sweat-shirts mis en avant</h2>

        <div class="product-grid">
            {% for sweatshirt in highlightedSweatshirts %}
                <div class="product-card">
                    <img src="{{ asset('images/' ~ sweatshirt.image) }}" alt="{{ sweatshirt.name }}">
                    <h3>{{ sweatshirt.name }}</h3>
                    <p>{{ sweatshirt.price }} €</p>

                    {% if app.user %}
                        <a href="{{ path('product_show', {id: sweatshirt.id}) }}" class="btn">Voir</a>
                    {% else %}
                        <p>Connectez-vous pour voir les détails</p>
                    {% endif %}
                </div>
            {% else %}
                <p>Aucun produit mis en avant pour l'instant.</p>
            {% endfor %}
        </div>
    </section>

    <section class="about-us">
        <h2>Qui sommes-nous ?</h2>
        <p>Notre entreprise est spécialisée dans la création et la vente de sweat-shirts de qualité, adaptés à tous les styles et toutes les tailles.
        Nom de la société : Stubborn
        Adresse : Piccadilly Circus, London W1J 0DA, Royaume-Uni
        Adresse de contact : stubborn@blabla.com
        Slogan: Don't compromise on your look
</p>
    </section>
{% endblock %}
